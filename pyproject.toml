[project]
name = "ecommerce-analytics-warehouse"
version = "0.1.0"
description = "A local dbt Core and DuckDB analytics warehouse project."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "dbt-core==1.11.10",
    "dbt-duckdb==1.10.1",
]
